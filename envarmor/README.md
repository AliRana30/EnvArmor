# envarmor

Offline-first CLI that detects hardcoded secrets before they are committed.

## Usage

```bash
npx envarmor scan
```

## Commands

- `envarmor scan [path]`
- `envarmor init`
- `envarmor login --key <api-key>`
- `envarmor protect`
